
/*
 * NodeHandle in Kalman is local.
 * Load yaml parameter to your node's namespace. 
 */


#include <iirob_filters/kalman_filter.h>

iirob_filters::MultiChannelKalmanFilter<double>* filter;
filter = new iirob_filters::MultiChannelKalmanFilter<double>();

std::vector<double> init_state_vector;
/* ... setting init_state_vector vector ... */
bool configure_result = filter->configure(init_state_vector);

std::vector<double> in, out;
/* ... setting in vector ... */
bool update_result = filter->update(in, out);

/* ... using out vector ... */